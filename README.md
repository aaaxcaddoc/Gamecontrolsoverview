
  # Game Controls Overview

  This is a code bundle for Game Controls Overview. The original project is available at https://www.figma.com/design/oxVLDlsasxWiuorkv6oniw/Game-Controls-Overview.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  